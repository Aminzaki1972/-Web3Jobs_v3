name: Restore company dashboard

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  restore:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Automatic dashboard restoration is disabled to prevent subscription code conflicts.'
